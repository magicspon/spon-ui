{% import '@helpers' as h %}
{% extends '@layout' %}

{# @sandbox #}


{% block route %}data-route{% endblock %}	

{% block content %}
<div data-barba="container" class="trans page-child" data-barba-namespace="{{ title }}">
	<div class="relative h-screen">
		<div style="{{ style }}" class="bg-{{ color }} absolute" id="counter" data-behaviour="counter">
			<button>Add</button>
			<div id="value">value</div>
			<h1>{{ title }}</h1>
		</div>
	</div>
</div>

{% endblock %}
